{
  "name": "auth-app",
  "version": "1.0.0",
  "description": "Base for an app implementing login using oauth",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/front",
    "apps/api",
    "apps/auth-provider"
  ],
  "dependencies": {
    "pm2": "^5.4.2"
  },
  "devDependencies": {
    "bun": "^1.1.30",
    "typescript": "^5.6.3"
  }
}
