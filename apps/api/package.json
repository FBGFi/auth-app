{
  "name": "api",
  "version": "1.0.0",
  "description": "Backend for the application",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "deploy": "pm2 start ecosystem.config.cjs --only dev-api",
    "build": "bun build ./src/index.ts --outdir dist --target node",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/koa__router": "^12.0.4"
  },
  "dependencies": {
    "@koa/router": "^13.1.0",
    "koa": "^2.15.3"
  }
}